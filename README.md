# Verbex
Verbex is a statically-typed, LLVM-backed programming language designed for developers who want the readability of Python, the performance discipline of C, and the safety guarantees of Rust—without compromise.  At its core, Verbex is built on a simple idea: code should be explicit, predictable, and impossible to misinterpret.
